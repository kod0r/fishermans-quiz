---
command: /diff-review
description: Pre-commit review of uncommitted changes
---

# /diff-review

Runs `git diff` and `git diff --staged`, then delegates to `reviewer` for inline analysis.

Usage: `/diff-review`
